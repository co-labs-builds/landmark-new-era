# Automations Punchlist — Team Verification Pass

Every automation (n8n workflow or native Ontraport automation rule) touched by the CS Dashboard + Member Portal build, regardless of whether it's already been reported as "built" or "working" during the build sessions. **Nothing here should be taken on faith** — this list exists specifically so the team can independently re-verify each item after the build wraps, including things Claude/Tobin said were done. Organized by confidence tier, not by feature area.

Last compiled: 2026-08-11. Tier 0 added 2026-08-12 (Take Attendance build).

---

## Tier 0 — Confirmed BROKEN, blocks the pilot if not fixed before Aug 14

- **Zoom Server-to-Server OAuth credential (`LM - Zoom TJC`, n8n credential id `xKXhhw7jCSrKtCsE`, `account_id` `FyRdEX3zTiyy32sKUJhdtg`) is currently failing token requests.** `POST https://zoom.us/oauth/token` with this credential/account_id returns a genuine, reproducible `400 {"reason":"Bad Request","error":"invalid_request"}` from Zoom itself — confirmed twice independently (once by the agent building the new Take Attendance workflow, once directly via a second live `execute_workflow` test, execution id `52766`, against the newly built `CS Dashboard : Take Attendance` workflow, id `t6AfyBKL6d68pK1L`).
  - **This is the exact credential + account_id the existing production `LM | Zoom | Live Attendance Poller` and `LM | Zoom | Meeting Run Start Detector` workflows already use** — so this isn't isolated to the new workflow. If it's genuinely broken, it would break Layer 1 live presence, the Layer 3 day-level minutes threshold (computed inline in the same poller), and the Final Attendance Poller too — the entire attendance pipeline for the live pilot, not just Take Attendance.
  - **Could not be confirmed one way or the other from existing production execution history**: checked directly, every recent execution of the two pollers above exited before ever reaching the Zoom API call, because there are currently zero active rows in the `zoom_meeting_runs` data table (nothing live to poll right now). So it's possible this credential has been silently broken for a while without anything noticing, since nothing has actually exercised it against a real live meeting recently.
  - Credential secrets are redacted from MCP/API tool access, so the root cause can't be diagnosed further this way (could be an expired/rotated client secret, a deactivated Zoom app, a mismatched `account_id`, or a wrong app type for this OAuth grant).
  - **Action needed**: whoever owns the Zoom Server-to-Server app in the Zoom Marketplace / App Management needs to check the app's status and secret directly, then confirm a real token request succeeds, ideally against a real/test live Zoom meeting before Aug 14. A second candidate credential exists in the n8n instance (`Zoom S2S - Test`, id `DgBbyDhAx9Iqpsgl`) but its name suggests it isn't the production one either — don't assume it's a ready substitute without checking.
  - Top priority item on this whole list — everything else here is lower urgency than this one.

---

## Tier 1 — Claimed/reported working, but NOT independently re-verified this pass

These were either confirmed by the client verbally, confirmed once earlier in the build and never re-checked, or inferred from indirect evidence (execution history, field behavior) rather than read directly. Re-check all of these.

- **WBO auto-set (`registrations.f2688` "Well Being Out").** Client stated (2026-08-11) this is "an automation based on the LDP reasoning" — i.e. should mirror the PDD-doc derivation (`f2293` Left The Course=true AND `f3061` WBO Reason ≠ "Other (not WBO)"). **Never independently inspected** — no n8n/Ontraport-automation-rule read was done to confirm this automation actually exists or that its logic matches the stated rule. If it's wrong or missing, the Master Stats WBO tile and the Roster WBO badge will both be silently wrong.

- **Attendance-tracking n8n workflow set.** Client says fully built against the test event, needs only a test→production credential swap. Never inventoried in detail — we have workflow *names* only, no confirmed mapping of which one does what: `LM | Zoom | Final Attendance Poller`, `Attendance QA Cleanup`, `Attendance Threshold Reconciler`, `Process Participant Attendance`, `Final Attendance Reconciliation`, `Meeting Run Start Detector`, `Seed Scheduled Meeting Runs`, `Late Outreach Detector`. Needs: (1) confirm each workflow's actual function against its name, (2) confirm which credential(s) need the test→production swap, (3) do the swap, (4) confirm a real check-in flows through end-to-end afterward. **Update 2026-08-11: the attendance architecture is now understood as 3 distinct layers (see below) — use that breakdown to figure out which of these 8 workflow names maps to which layer.**

- **Layer 1 — `f2853` "Currently Present" live poller.** Client confirmed the *design* (15-min poll, active-session-windows-only, 30-min/2-miss threshold to uncheck, pure alert signal — no record other than `f2853` itself is touched) but **did not confirm this specific poller is already built** — only confirmed a *different* automation (the day-level minutes-threshold one, now Tier 2 below) exists. Don't assume this is covered by that one; check directly whether a poller matching this exact behavior (15-min cadence, session-window-aware, 30-min uncheck threshold) exists among the 8 named workflows above, or whether it still needs to be built.

- **Seminar/AC registration → registration checkbox writeback.** When someone registers for a Seminar or Advanced Course, something needs to check `registrations.f2303` "SEM Registration" / `f2302` "AC Registration" on the record. Asked about this directly this session — blocked on n8n access (LMN8N connector unauthenticated in this session despite being connected in a different chat). **Completely unverified whether this automation exists at all.** This directly feeds the new Master Stats Seminar/AC Registrant % tiles — if it's missing or broken, those stats will read wrong with no obvious symptom.

- **Seminar/AC Reg-Open email automation.** Per earlier build notes, toggling `events.f3104`/`f3105` (Seminar/AC Reg Open) is supposed to fire "a separate, already-existing Ontraport automation that sends an email" to participants. This was stated by the client, never opened and inspected directly. Also unconfirmed: whether `f3167` "\"Gift a Forum\" Open" (added later, alongside the other two) has an equivalent email automation, or was never wired since it's newer.

- **`REG : Create Registration at Checkout (Pilot)`** (n8n id `hTQ5oFvaHxeEWHBR`) — real, live, payment-adjacent production workflow. Was modified mid-build (added a parallel `Set Portal Home URL` branch). Verified once via structural diff + one real test checkout at the time, but this is the highest-blast-radius workflow in the whole project (it's what actually creates real paid registrations) — worth a dedicated regression pass by someone other than the person who edited it.

---

## Tier 2 — Confirmed built and independently verified (lower priority, but re-confirm after final install)

- **Layer 3 — day-level minutes-threshold automation.** Client explicitly confirmed (2026-08-11) this already exists and is live: purely per-day cumulative Zoom presence, 300 sec (5 min) total that day → flips `registrations.f2801`/`f2802`/`f2803` "Attended Day X". This is the **sole authority** for LDP/WBO eligibility, Forum Completed, and refund status — nothing else in the attendance system should touch these three fields. Not per-session (client confirmed explicitly). Even though the client confirmed this one verbally, it's still worth the team spot-checking it actually writes only `f2801`–`f2803` and doesn't also touch any of the new per-session fields below, since those need to stay Layer 2's exclusive territory.
- **`PORTAL : Ably Publish`** (n8n id `vWIsMj7kouwDhddN`) — the 6 Course Materials fields + day-advance (7 total Ontraport automation rules) confirmed firing correctly via real n8n execution history (`user-agent: "Ontraport Webhook"`, not just manual tests). **One leg still not verified in one continuous real chain**: a real Ontraport automation firing → n8n → Ably → the During-event page updating live in an actual open browser tab, all in a single test. Verified in pieces, not end-to-end together.
- **`PORTAL : Ably Token Auth`** (n8n id `jKY55TShApwyZ2mP`) — tested: real authorized case, real unauthorized case (bogus event ID), malformed-input case. Confirmed working.
- **`PORTAL : My Account : Write-Back`** (n8n id `dTqrDpYs9XWMwehp`) — confirmed working end-to-end including photo upload, after multiple real bugs found and fixed (merge-node mode bug, wrong branch selected for binary data). Given how many rounds of bugs this one had, worth one more clean regression test (text-only save, photo save, refresh-persistence) before considering it closed.
- **Login Redirects (participant)** — native Ontraport automation rule, confirmed live: redirects to the correct personalized Portal Home URL on login, logout correctly clears session. Working.
- **`Set Portal Home URL` node inside the checkout workflow** — see Tier 1 note above (bundled with the parent workflow's regression pass).

---

## Tier 3 — Confirmed NOT built, real known gaps

- **Layer 2 — Take Attendance — UPDATE 2026-08-12: now built, move this to Tier 1 for re-verification, not Tier 3.** `CS Dashboard : Take Attendance` (id `t6AfyBKL6d68pK1L`) + `CS Dashboard : Attendance Reconcile Sweep` (id `hVemNaYj1wAqZOe0`), both active/published. Validation/auth paths and the "no active meeting" path were verified live; the actual present-participant→field-written path could NOT be verified end-to-end since no live Zoom meeting existed during the build — and is separately blocked on the Tier 0 Zoom credential issue above regardless. Re-verify fully once Tier 0 is resolved and a real/test meeting is available. Original spec below, still accurate:
  - CS clicks one "Take Attendance" button → floating menu (S1/S2/S3/S4) → confirm → fresh Zoom Live Dashboard API poll at click-time (deliberately independent of Layer 1's `f2853` cached state, per client's explicit choice).
  - Present participants → check the matching per-session field: `registrations.f3193`(D1S1)/`f3194`(D1S2)/`f3195`(D1S3)/`f3196`(D1S4)/`f3197`(D2S1)/`f3198`(D2S2)/`f3199`(D2S3)/`f3200`(D2S4)/`f3201`(D3S1)/`f3202`(D3S2)/`f3203`(D3S3)/`f3055`(D3S4). Day+session resolved off `events.f3025`.
  - Not present on first poll → check `f3062` "FS Late Arrival" (drives a "Late" badge on the roster row).
  - Automatic re-poll 15 min later: now present → check the session field, clear Late. Still absent → leave unresolved for the CS.
  - CS resolves manually via record status → edit → `f3191` "Attendance Status" = Absent-Excused (client indicated a conversation note gets logged here, likely `f3190` "FS LATE NOTE" — not 100% nailed down as the canonical field for this specific note vs. the override note below) or Absent-NCNS.
  - Separate path for Zoom/Participant-ID mismatches: CS uses the record's ••• kebab → Attendance Override → mark present + note → `f3190`.
  - **Cross-cutting rule**: when CS escalates a drop-off to LDP/WBO via Attendance Override, that action also retroactively flips the current/most-recent session's ATTENDED field, so the session record and course-status record don't tell conflicting stories.
  - **Open field question, not yet resolved**: `f3054` "D3S3 Checkpoint" (used by the already-confirmed Forum Completed formula) vs. the new `f3203` "D3S3 ATTENDED" — two fields covering the same slot. Need to confirm whether Forum Completed's formula should switch to `f3203`, or `f3054` stays authoritative and `f3203` is just for display.
- **Staff Zoom matching for Device & Zoom Reconciliation.** New `oEventTeam` fields (`f3215` Zoom Join Link, `f3216` Zoom Participant ID, `f3217` Staff Access, `f3218` Currently Present) exist, but nothing populates them yet. Design: extend the existing registrant Zoom-matching poll to also check for Staff Access=true `oEventTeam` matches (by name/email) before falling through to registrant matching; write the matched Zoom Participant ID to `f3216`; set `f3218` the same way `registrations.f2853` already gets set. Feeds the Master Stats "Device & Zoom Reconciliation" card's Staff/Observed counts. Fully spec'd 2026-08-12, not yet built — blocked on LMN8N connector access same as everything else n8n-side this session.
- **Dashboard toggle → Ontraport write path.** The CS Dashboard's Course Materials release toggles and any Day-advance control have **zero write-back wiring** as of this session (confirmed via grep — no `fetch`/webhook call anywhere). The plan is one shared webhook (same semantic-key-lookup pattern as `PORTAL : Ably Publish`), but it doesn't exist yet.
- **Announcement fields not covered by the Ably publish trigger scope.** The current 7 Ontraport automation rules feeding `PORTAL : Ably Publish` only watch the 6 Materials fields + day-advance. The 3 announcement fields (`events.f3104`/`f3105`/`f3167` — Seminar/AC/Gift Open) have no equivalent trigger rule and aren't in the Ably Publish lookup table — so even once the dashboard can write to them, nothing will live-push the change to participants.
- **Designated vs. Alternate auto-computation.** No automation exists to compare the specific Seminar/AC a registrant signed up for (new `registrations.f3185`/`f3186` Seminar/AC Title fields) against the event's Designated Seminar 1–3/AC (`events.f3133`–`f3136`) and set `registrations.f2885`/`f2890` (Seminar/AC Course Choice) accordingly. Currently these would have to be set by hand.
- **Staff Login Redirect.** Blocked on the CS Dashboard's own "Home" page not existing yet as a real installed Ontraport page — once it does, this is just adding a second native Login Redirects rule conditioned on `contacts.f2771` "Staff Access"=true. Not built, not blocked on anything technical, just sequencing.
- **`PORTAL : Set Portal Home URL` standalone webhook** (n8n id `oWS8MWK9Rn27urnd`) — built, functional, but now redundant since the checkout-workflow integration replaced its purpose. Left running rather than deleted. Team should decide: keep as a manual-correction tool, or retire it.

---

## Not an automation gap, but adjacent and worth a mention

- **Minor → SE.** By design this is a manual CS action through the Override Classification modal (client-confirmed: "one single action always sets Status+Reason+checkbox together"), not an automated trigger. Flagging only so nobody mistakes its absence from this list as an oversight.
