# One-off paste-ready extracts for manual Ontraport Page Builder install.
# Mechanical snapshots of member-portal.html at generation time -- would
# drift from source immediately on the next edit. Regenerate fresh
# (see C:\Users\chris\.claude\plans\review-the-docs-and-mutable-moler.md)
# rather than trusting a committed copy.
INSTALL-header-code.html
INSTALL-body-block.html

# n8n Public API key — S2S credential, never commit
.env

# Claude Code state carried over from the previous build machine during the
# 2026-08-13 migration (see HANDOFF.md). The archive and its unpacked folder
# both contain .claude/.credentials.json — a live OAuth credential — plus the
# full session transcript history. This repo is PUBLIC: a stray `git add -A`
# would publish all of it. Neither belongs in the repo at all; these entries
# are a backstop, not permission to keep them here.
claude old machine.zip
claude old machine/
